name: 'My custom action'
description: 'Be a composite action'
runs:
  using: "composite"
  steps:
    - env:
        GHARep: ${{ github.workflow }}
        GHARef: ${{ github.ref }}
      run: |
        echo "${{ toJson(github) }}"
      shell: bash
